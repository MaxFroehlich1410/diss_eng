# Krotov Paper Archive

## Overview

This folder contains archived manuscript and bibliography sources related to the repository's experimental results. It is kept separate from the runnable benchmark folders in `experiments/`.

## Notes

- Treat the contents here as paper-support material rather than executable experiment code.
- The canonical runnable experiments now live in the sibling folders under `experiments/`.
