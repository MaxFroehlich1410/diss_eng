# Two-Moons Dense-Angle Benchmark

## Overview

Benchmark the dense-angle circuit with the shared optimizer stack.

## Run

```bash
python -m experiments.two_moons_dense_angle.run
python -m experiments.two_moons_dense_angle.build_report
```

## Outputs

Generated artifacts are written under `results/` and are ignored by git.
