# Alternative Two-Moons Models

## Overview

Benchmark and visualize alternative QML model families on the two-moons task.

## Run

```bash
python -m experiments.two_moons_alternative_models.run
python -m experiments.two_moons_alternative_models.build_figures
```

## Outputs

Generated artifacts are written under `results/` and are ignored by git.
